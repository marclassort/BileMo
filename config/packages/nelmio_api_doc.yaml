nelmio_api_doc:
  documentation:
    info:
      title: BileMo
      description: API to manage products and users
      version: 1.0.0
    servers:
      -urls: https://localhost:8000/api/
  areas: # to filter documented areas
    path_patterns:
      - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
